{
  "name": "patternplate-language-server",
  "description": "Implementation of a patternpalte language server.",
  "version": "0.0.1",
  "author": "SinnerSchrader",
  "license": "MIT",
  "engines": {
    "node": "*",
    "vscode": "^1.4.0"
  },
  "dependencies": {
    "globby": "^6.0.0",
    "json-to-ast": "^1.2.13",
    "vscode-languageserver": "^2.2.0"
  },
  "devDependencies": {
    "typescript": "^1.8.10",
    "vscode": "^0.11.0"
  },
  "scripts": {
    "postinstall": "node ./node_modules/vscode/bin/install",
    "compile": "installServerIntoExtension ../client ./package.json ./tsconfig.json && tsc -p .",
    "watch": "installServerIntoExtension ../client ./package.json ./tsconfig.json && tsc --watch -p ."
  }
}
